/* global describe, it */

(function () {
    'use strict';

    describe('A messages collection', function () {
        describe('and it', function () {
            it('should', function () {

            });

            it('should', function() {

            })

            it('should', function() {

            })
        });
    });
})();
